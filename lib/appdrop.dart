/*import 'package:flutter/widgets.dart';
import 'package:treedraw/treeutil.dart';

class AppDrop {
  void init(State state) {}
  void dispose() {}

  factory AppDrop(TreeUtil treeutil) => getAppDrop(treeutil);
}

AppDrop getAppDrop(TreeUtil treeutil) => throw UnsupportedError("message");*/
